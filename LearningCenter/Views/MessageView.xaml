<UserControl x:Class="LearningCenter.Views.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LearningCenter.Views"
             mc:Ignorable="d" 
             Height="{Binding ViewHeight}"
             Width="{Binding ViewWidth}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="8" Padding="4" Background="LightBlue" BorderBrush="Transparent" BorderThickness="0">
        <Grid Background="Transparent">
            <TextBlock HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap" FontSize="11" FontFamily="Microsoft Yahei" Text="{Binding MsgText}"/>
        </Grid>
    </Border>
   
</UserControl>
